% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delay_estimation.R
\name{extractParsExt}
\alias{extractParsExt}
\title{Extract certain parameters from a given named parameter vector.}
\usage{
extractParsExt(
  parV,
  distribution = c("exponential", "weibull"),
  twoPhase = NULL,
  group = NULL,
  isTransformed = NULL,
  transform = FALSE
)
}
\arguments{
\item{parV}{numeric parameter vector (for single group or two groups)}

\item{distribution}{character name. Exponential or Weibull}

\item{twoPhase}{logical. Do we model two phases?}

\item{group}{character. Extract only parameters for specified group. Recognizes 'x' and 'y'. Other values are ignored.}

\item{isTransformed}{logical. Are the parameters transformed? If \code{NULL} (default) it is deduced from the parameter names.}

\item{transform}{logical. Transform the parameters!?}
}
\value{
requested parameters as named numeric vector
}
\description{
This routine handles parameter vectors for one or two groups. To this end, it parses the parameter names.
Bound parameters (cf. \verb{bind=}) are deduced from the naming: they lack the .x or .y suffix.
it can also transform the given parameters from the standard form (as used in the delayed distribution functions)
to the internal transformed parametrization as used by the optimization function, and vice versa.
}
\details{
When parameters for a single group are requested the parameters are always returned in the canonical order of the distribution.
Otherwise, the order of parameters is unchanged.
It parses the parameter names to find out if it is twoPhase, twoGroup and, when \code{transform=TRUE}, which direction to transform.

This is an internal helper function
used in \code{\link[=delay_model]{delay_model()}}, in the factory method \code{\link[=objFunFactory]{objFunFactory()}} and in \code{\link[=coef.incubate_fit]{coef.incubate_fit()}}.
}
